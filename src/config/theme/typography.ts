// typography.ts
// Define la configuración tipográfica global (fuentes, tamaños, pesos, etc.)
// Puedes usar Google Fonts o locales según tu necesidad.
const typography = {
  fontFamily: ['Inter', 'sans-serif'].join(','), // Fuente principal de la app
  fontSize: 14,                                 // Tamaño base de texto (rem = 14px)
  fontWeightBold: 600,                          // Peso de letra para textos en negrita
};

export default typography;
